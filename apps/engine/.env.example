# LinkFlow Go Engine - Environment Configuration
# Copy this file: cp .env.example .env
# Used by: apps/engine/docker-compose.yml (standalone mode)

# ============================================
# Database
# ============================================
POSTGRES_USER=linkflow
POSTGRES_PASSWORD=       # REQUIRED: Generate with: openssl rand -base64 24
POSTGRES_DB=linkflow
# Host is automatically set to 'linkflow-postgres' in docker-compose

# ============================================
# Redis
# ============================================
REDIS_PASSWORD=          # REQUIRED: Generate with: openssl rand -base64 24
# Host is automatically set to 'linkflow-redis' in docker-compose

# ============================================
# Security Secrets (Critical)
# ============================================
# Shared secret with Laravel API for callback authentication
LINKFLOW_SECRET=         # REQUIRED: Match root .env (openssl rand -base64 32)

# JWT secret for Engine API authentication
JWT_SECRET=              # REQUIRED: Match root .env (openssl rand -base64 32)

# ============================================
# Scaling & Config
# ============================================
LOG_LEVEL=info
ENGINE_PARTITION_COUNT=16

# ============================================
# Multi-Region (Optional)
# ============================================
# CELL_ID=cell-1
# REGION=us-east-1

# ============================================
# External Integrations (Optional)
# ============================================
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-...
# STRIPE_SECRET_KEY=sk_test_...
# GITHUB_TOKEN=ghp_...
