# LinkFlow - Environment Variables (Single Source of Truth)
# Copy this file: cp .env.example .env
# Then fill in REQUIRED values before starting.
#
# Generate secrets:
#   openssl rand -base64 24   (for passwords)
#   openssl rand -base64 32   (for secrets/keys)
#   php artisan key:generate  (for APP_KEY, run inside API container)

# ============================================
# Database (Required)
# ============================================
POSTGRES_USER=linkflow
POSTGRES_PASSWORD=          # REQUIRED: openssl rand -base64 24
POSTGRES_DB=linkflow
POSTGRES_PORT=5432

# ============================================
# Redis (Required)
# ============================================
REDIS_PASSWORD=             # REQUIRED: openssl rand -base64 24
REDIS_PORT=6379

# ============================================
# Security (Required)
# ============================================
LINKFLOW_SECRET=            # REQUIRED: openssl rand -base64 32 — shared API↔Engine
JWT_SECRET=                 # REQUIRED: openssl rand -base64 32 — Engine JWT signing
APP_KEY=                    # REQUIRED: php artisan key:generate — Laravel encryption

# ============================================
# Engine Configuration
# ============================================
ENGINE_PARTITION_COUNT=16
LOG_LEVEL=info

# ============================================
# SSL (Production Only)
# ============================================
SSL_CERT_PATH=./infra/nginx/ssl

# ============================================
# Scaling (Production Only)
# ============================================
# CELL_ID=cell-1
# REGION=us-east-1
# ELASTICSEARCH_URL=http://elasticsearch:9200

# ============================================
# External Integrations (Optional — uncomment to enable)
# ============================================
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-...
# STRIPE_SECRET_KEY=sk_...
# STRIPE_PUBLISHABLE_KEY=pk_...
# GITHUB_TOKEN=ghp_...
# SENTRY_DSN=https://...
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=linkflow-storage
